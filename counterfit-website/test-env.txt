# Test Environment Configuration
# Copy these to your .env.local file for testing

# Backend URL (set to localhost for testing, or your working backend)
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# Email Configuration
EMAIL_SERVICE=sendgrid
EMAIL_API_KEY=SG.your_sendgrid_api_key_here
EMAIL_FROM=helpcounterfit@gmail.com
EMAIL_FROM_NAME=Counterfit

# For now, you can also set this to disable backend calls:
# NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
