// Configuration for the frontend application

export const config = {
  apiUrl: process.env.NEXT_PUBLIC_BACKEND_URL || 'https://counterfit-backend.onrender.com',
  // Add other config options here
}
